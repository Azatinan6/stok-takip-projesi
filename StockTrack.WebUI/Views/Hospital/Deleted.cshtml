@using StockTrack.Dto.Hospital
@model IEnumerable<HospitalDeletedDto>

@{
    ViewData["Title"] = "Silinen Hastaneler";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h4>Silinen Hastaneler</h4>
<hr />

<table id="DataTables" class="display" style="width:100%">
    <thead>
        <tr>
            <th>#Id</th>
            <th>Kurum Adı</th>
            <th>Şube</th>
            <th>HBYS</th>
            <th>Versiyon</th>
            <th>Silinme Tarihi</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @{
            int count = 1;
        }
        @foreach (var hospital in Model)
        {
            <tr>
                <td>@(count++)</td>
                <td>@hospital.Name</td>
                <td>@hospital.Branch</td>
                <td>@(hospital.HbysName ?? "-")</td>
                <td>@(hospital.HbysVersion ?? "-")</td>
                <td>@hospital.DeletedDate?.ToString("dd MMMM yyyy HH:mm")</td>
                <td>
                    <form asp-controller="Hospital" asp-action="Restore" asp-route-id="@hospital.Id" method="post" onsubmit="return confirm('Bu hastaneyi geri yüklemek istediğinize emin misiniz?');">
                        <button type="submit" class="btn btn-success btn-sm">
                            <i class="mdi mdi-restore me-1"></i> Geri Yükle
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="mt-3">
    <a asp-controller="Hospital" asp-action="Index" class="btn btn-secondary">
        <i class="fa fa-arrow-left"></i>Hastane Listesine Dön
    </a>
</div>





